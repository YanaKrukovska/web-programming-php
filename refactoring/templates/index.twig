<?php include_once("refactoring.php") ?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        #book-list, h1 {
            text-align: center;
        }

        h1 {
            color: green;
        }
    </style>
</head>
<body>

<h1>BOOK CATALOGUE</h1>

<div id="book-list">
    {% for book in books %}
        {% if book.year > 2015 %}
            <p><b>{{ book.title }} - {{ book.author }} - {{ book.year }}</b></p>
        {% else %}
            <p>{{ book.title }} - {{ book.author }} - {{ book.year }}</p>
        {% endif %}
    {% endfor %}
</div>

</body>
</html>
